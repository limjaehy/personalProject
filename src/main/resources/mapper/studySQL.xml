<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="study">
	<select id="selectStudyList" resultType="s">
		select
			study_number as studyNumber.
			study_category_number as studyCategoryNumber.
			study_title as studyTitle,
			study_content as studyContent,
			stydy_imagepath as studyImagePath,
			study_date as studyDate
		from study
		order by study_date desc
	</select>
</mapper>
